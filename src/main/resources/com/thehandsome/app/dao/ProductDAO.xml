<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thehandsome.app.dao.ProductDAO">

	<select id="getProductInfo" parameterType="String" resultType="ProductDTO">
		<![CDATA[
		SELECT id, brandId, sex, title, price, maincategoryId, subcategoryId, shortDetail, longDetail
		FROM product
		WHERE id like #{id}
		]]>
	</select>

	
</mapper>